class Solution:
    
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        for i in range(0,len(nums)):
            for j in range(i+1,len(nums)):
                if target==nums[i]+nums[j]:
                  print('i=',i,'i=',j,nums[i]+nums[j])
                  return [i,j]
        
    
pan=Solution()
pan.twoSum(nums=[2,7,11,15],target=9)